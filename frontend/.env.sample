REACT_APP_API_URL=http://localhost:3001
REACT_APP_API_ENDPOINTS='{}'

# Written by bin/generate-env.sh; the frontend reads only VITE_* vars.
# Base URL of bin/proxy-server.js, which routes /api/<service>/<route> to Lambda.
VITE_API_URL=http://localhost:3001
